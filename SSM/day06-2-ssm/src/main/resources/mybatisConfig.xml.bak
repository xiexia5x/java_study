<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!--配置加载外部的properties文件，定义全局变量-->
    <!--<properties resource="jdbc.properties"/>-->
    <!--开启驼峰映射-->
    <!--<settings>
        &lt;!&ndash;table: user_name_xx自动映射到pojo类属性：userNameXx&ndash;&gt;
        <setting name="mapUnderscoreToCamelCase" value="true"/>
    </settings>-->
    <!--取别名-->
    <!--<typeAliases>
        &lt;!&ndash;别名：小驼峰（类名首字母小写）&ndash;&gt;
        <package name="com.heima.pojo"/>
    </typeAliases>
-->
    <!--配置分页插件-->
    <!--<plugins>
        <plugin interceptor="com.github.pagehelper.PageInterceptor">
            &lt;!&ndash;配置分页插件作用的数据库&ndash;&gt;
            <property name="helperDialect" value="mysql"/>
                &lt;!&ndash;
                   当前页小于1时，查询第一页；
                   当前页大于最大页是查询最后一页
               &ndash;&gt;
            <property name="reasonable" value="true"/>
        </plugin>
    </plugins>-->
  <!--  <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="${jdbc.driverClass}"/>
                <property name="url" value="${jdbc.url}"/>
                <property name="username" value="${jdbc.userName}"/>
                <property name="password" value="${jdbc.password}"/>
            </dataSource>
        </environment>
    </environments>-->
    <!--<mappers>
        &lt;!&ndash;<mapper resource="org/mybatis/example/BlogMapper.xml"/>&ndash;&gt;
        <package name="com.heima.dao"/>
    </mappers>-->
</configuration>